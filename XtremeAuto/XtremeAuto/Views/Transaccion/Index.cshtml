@model IEnumerable<XtremeAuto.Models.Transaccion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Fecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tarjeta.NumeroTarjeta)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Venta.IDCliente)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tarjeta.NumeroTarjeta)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Venta.IDCliente)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDTransaccion }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDTransaccion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDTransaccion })
        </td>
    </tr>
}

</table>
